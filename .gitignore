# Live values. Only .env.example is tracked, and it must never carry a real value.
# No leading slash, so this matches at every depth: each gateway project keeps its
# OWN .env beside its compose.yml, and there is no repo-root one any more.
.env
.envrc

# Credentials of any kind, in case one is ever written here by hand.
*.key
*.pem
.agent-token

# Machine-local Claude Code overrides. settings.json IS tracked; this one never is
# — it is where a gateway key would land if one were ever put in an `env` block.
.claude/settings.local.json

# PERSONAL, PER-MACHINE CONFIGS. `GATEWAY_ENGINE` names any file in config/, so a gitignored
# one is how you serve YOUR disk without committing it: `GATEWAY_ENGINE=lukas` reads
# config/lukas.yaml on either gateway. These describe one laptop and would be wrong on
# another, which is the whole reason they are not tracked. The two must be kept in step by
# hand — neither gateway reads the other's.
litellm/config/lukas.yaml
envoy/config/lukas.yaml

# Each gateway's tests/ is its own uv project. The lockfile IS tracked; the
# environment it builds is not.
.venv/
__pycache__/

.DS_Store

# The benchmark's raw per-call timings. A measurement of THIS machine on one day,
# so it is a local fact; the numbers worth keeping are summarised in README.md.
benchmark/results.json
