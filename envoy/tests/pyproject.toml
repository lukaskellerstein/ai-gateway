[project]
name = "ai-gateway-tests"
version = "0.1.0"
description = "Scripts that drive both gateways through the OpenAI client"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.20.0",
    "python-dotenv>=1.0.1",
]
