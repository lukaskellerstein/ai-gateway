# =============================================================================
# ai-gateway — the machine-wide LLM gateway
# Works with BOTH `podman compose up -d` and `docker compose up -d`.
#
# Derived from ~/Projects/Github/lukaskellerstein/ai-agent-platform/deploy/compose,
# reduced to what a GLOBAL gateway owns. Five services, and each is load-bearing:
#   litellm      the OpenAI-compatible endpoint every project on this machine calls
#   postgres     virtual keys, spend logs and budget ceilings. Not published.
#   mlflow       the SAME aliases through the MLflow AI Gateway, for comparison
#   mlflow-seed  a one-shot that writes MLflow's endpoint list in. Exits.
#   discover     a one-shot that writes LiteLLM's config when auto-discovery is on,
#                and does nothing at all when it is off — the default. Exits.
#
# THREE WORDS IN .env DECIDE WHAT RUNS. Nothing else in this file needs editing to
# get a different stack:
#
#   COMPOSE_PROFILES   litellm | mlflow | litellm,mlflow | all       WHICH GATEWAY
#   GATEWAY_ENGINE     lms | unsloth | ollama | openrouter | openai  WHICH ENGINE
#   GATEWAY_DISCOVERY  (empty) | on                                  WHICH MODELS
#
# THE THIRD IS OFF BY DEFAULT AND THAT IS THE POINT. With it empty — a fresh clone,
# and every stack that ran before 2026-09-03 — each gateway serves exactly the two
# or three aliases its hand-written file names, and those files stay in the repo as
# the worked example of configuring this by hand.
#
# Set it and each gateway ADDS every model its engine holds on disk, discovered at
# start over the engine's own HTTP API. LMStudio and Ollama both report the models
# they have, loaded or not, and both JIT-load, so a not-loaded model answers on the
# first call. THE HAND-WRITTEN ALIASES ARE NEVER REPLACED: turning discovery on can
# only add names, so nothing a project already calls can break.
#
# DISCOVERY IS LOCAL-ONLY. `lms`, `unsloth` and `ollama` are free, so listing
# everything costs a longer list. OpenRouter and OpenAI bill a real account per
# model, so they keep hand-written lists and MONEY IS NEVER DISCOVERED.
#
# COMPOSE_PROFILES is compose's own variable and it gates the two gateway services
# below; GATEWAY_ENGINE is interpolated into a filename on one side and passed as
# environment on the other. postgres carries no profile, so it always starts —
# both gateways need it.
#
# ONE ENGINE AT A TIME, ON PURPOSE. There is no list, no `all` and no starter/full
# split: the gateway serves one engine's two or three aliases and nothing else. To
# compare two engines, change the word and run `up -d` again — the aliases are named
# so only the prefix differs (`lms-26b` / `unsloth-26b` / `ollama-26b` /
# `openrouter-26b` are the same weights).
#
# AN ENGINE IS AN ENGINE, LOCAL OR HOSTED. `openrouter` and `openai` are engine names
# like `lms` is, and the alias prefix says which is which. Selecting one of those two
# is the ONLY way anything in the running stack can spend money.
#
# WITH NO .env AT ALL, `up -d` STARTS POSTGRES AND NOTHING ELSE, because a service
# with a profile is off until its profile is named. `cp .env.example .env` is the
# first line of the quick start for exactly this reason.
#
# TWO GATEWAYS, ONE VOCABULARY, TWO INDEPENDENT CONFIGS. LiteLLM stays the primary
# endpoint: it is what every project calls and the only one with virtual keys,
# spend logs and budget ceilings. MLflow serves the same alias names so the two can
# be compared on one machine — swap the base URL, keep the model name.
#
# EACH GATEWAY OWNS ITS OWN ALIAS LIST — one self-contained file per engine, named
# by the same word on both sides:
#
#   LiteLLM    litellm/<engine>.yaml   includes settings.yaml, declares its aliases
#   MLflow     mlflow/<engine>.py      an ENDPOINTS list, written in over the API
#
# MLflow's side reads nothing from LiteLLM's, so the `litellm` service and the
# whole litellm/ directory can be deleted and the MLflow gateway still comes up and
# serves. The price of that independence is that the two lists are maintained
# twice — the one word keeps them on the same ENGINE, but adding a model is still
# two edits, not one.
#
# The MLflow here is a GATEWAY, not a trace store for other projects. It traces
# what passes through ITS OWN endpoints and nothing else. `success_callback` in
# litellm/settings.yaml stays empty for the original reason: two projects sharing
# one experiment namespace makes "did this get better" ambiguous, which is the
# one question a trace store exists to answer. A project that wants tracing runs
# its own MLflow and traces client-side.
#
# WHAT WAS DELIBERATELY LEFT OUT
#   qdrant / nats / temporal / prometheus — none of them is a gateway concern.
#     The platform stack parks them behind profiles; this repo does not carry
#     definitions for services it does not run.
#   A custom litellm image — the platform builds one only to add `mlflow-skinny`
#     for its callback. With no callback, the stock image is the whole story, and
#     `up -d` needs no build step. Reintroduce ./litellm/Dockerfile the day a
#     callback needs a package the base image lacks.
#   A custom mlflow image — the stock one already ships psycopg2 and cryptography
#     (checked on ghcr.io/mlflow/mlflow:latest, MLflow 3.15.1), which is every
#     package the Postgres backend and the gateway's encrypted secrets need. So
#     this stack still has NO build step.
#
# THREE LOCAL ENGINES, ALL NATIVE ON THE HOST (Apple-Silicon GPU), none of them a
# container: LMStudio on 1234, Unsloth Studio on 8888, Ollama on 11434. All three
# bind 127.0.0.1 only, and the containers reach all three the same way — at
# host.containers.internal (Podman) / host.docker.internal (Docker). See .env.
# They share one GPU, so a 31B loaded in each is three copies of the same weights
# in memory, not one.
#
# HOST PORTS 24000 AND 25000 — a third band, chosen so this stack coexists with
# both project stacks rather than racing them for a port:
#   mlflow-tutorial    holds 4000, 5432, 5555, 3000, 6333/4, 7233, 8080, 9090
#   ai-agent-platform  holds the 1xxxx band (14000, 15000, ...)
#   ai-gateway         2xxxx — litellm 4000 -> 24000, mlflow 5000 -> 25000
# The failure this avoids is not a bind error, which is loud and obvious. It is
# the SILENT one: a health probe against localhost:4000 that another project's
# gateway answers, going green against a service from a different stack. Container-
# internal ports are unchanged — on the compose network nothing can collide.
# =============================================================================

# Every container becomes `ai-gateway-<service>-N`. Without this, compose names the
# project after this directory, which on a machine running several stacks says
# nothing about who owns what.
name: ai-gateway

services:
  # ---------------------------------------------------------------------------
  # Postgres — the gateway's own store: virtual keys, teams, spend logs.
  #
  # NO PUBLISHED HOST PORT, deliberately. Only litellm connects to it, over the
  # compose network. Publishing it buys nothing and guarantees a collision (5432
  # is mlflow-tutorial's). For a shell: `podman compose exec postgres psql -U postgres`.
  #
  # TWO databases live here. `litellm` comes from POSTGRES_DB below; `mlflow` comes
  # from postgres/init-databases.sh, because POSTGRES_DB takes exactly one name.
  # Both servers apply their own migrations on first startup, and neither can see
  # the other's tables.
  # ---------------------------------------------------------------------------
  postgres:
    image: docker.io/postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: litellm
      TZ: UTC
      PGTZ: UTC
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d litellm"]
      interval: 3s
      timeout: 3s
      retries: 15
    volumes:
      - postgres_data:/var/lib/postgresql/data
      # Creates the `mlflow` database. Postgres runs this ONLY when it initialises
      # an empty data directory, so on a volume that already exists it does
      # nothing — the file itself carries the one-liner for that case.
      - ./postgres/init-databases.sh:/docker-entrypoint-initdb.d/init-databases.sh:ro

  # ---------------------------------------------------------------------------
  # discover — writes LiteLLM's config when auto-discovery is on, then exits.
  #
  # A CONTAINER THAT EXITS IS THE EXPECTED STATE, exactly like mlflow-seed.
  #
  # THE THIRD WORD IN .env DECIDES WHETHER IT DOES ANYTHING:
  #
  #   GATEWAY_DISCOVERY   (empty) | on          default EMPTY
  #
  # Empty is the default, and then this exits 0 in about a second having written
  # nothing — litellm reads the hand-written litellm/<engine>.yaml exactly as it
  # always has. Set it and this asks the engine what it holds and writes
  # litellm/discovered-<engine>.yaml, which INCLUDES the hand-written file. So
  # discovery only ever ADDS aliases; nothing a project already calls can break.
  #
  # IT IS ON THE `litellm` PROFILE ONLY, and that is deliberate. The MLflow gateway
  # discovers for itself inside mlflow-seed, because its endpoints are database rows
  # and not a file. So `COMPOSE_PROFILES=mlflow` still starts no LiteLLM container
  # and no LiteLLM image is ever pulled — the profile independence is unchanged.
  #
  # IT USES THE LITELLM IMAGE because it needs a Python and that image already ships
  # one. The alternative was a third image pulled for a script with no dependency
  # outside the standard library. STILL NO BUILD STEP.
  #
  # ./litellm IS MOUNTED READ-WRITE HERE and read-only on litellm below: this is the
  # one thing in the stack that writes into the repo, and the file it writes is
  # gitignored.
  # ---------------------------------------------------------------------------
  discover:
    image: ghcr.io/berriai/litellm:main-stable
    restart: "no"
    profiles: ["litellm", "all"]
    entrypoint: ["python"]
    command: ["/app/discover/gateway_discovery.py"]
    volumes:
      - ./discover:/app/discover:ro
      - ./litellm:/app/config
    extra_hosts:
      # It dials the engine on the host, so it needs the same two names the
      # gateways do.
      - "host.docker.internal:host-gateway"
      - "host.containers.internal:host-gateway"
    environment:
      # The same two words the gateways read, plus the one that turns this on.
      GATEWAY_ENGINE: ${GATEWAY_ENGINE:-lms}
      GATEWAY_DISCOVERY: ${GATEWAY_DISCOVERY:-}
      LITELLM_CONFIG_DIR: /app/config
      # Where to ask. Only the three LOCAL engines are ever discovered — OpenRouter
      # and OpenAI bill a real account per model, so they keep hand-written lists.
      LM_STUDIO_API_BASE: ${LM_STUDIO_API_BASE:-http://host.containers.internal:1234/v1}
      UNSLOTH_API_BASE: ${UNSLOTH_API_BASE:-http://host.containers.internal:8888/v1}
      UNSLOTH_API_KEY: ${UNSLOTH_API_KEY:-}
      OLLAMA_API_BASE: ${OLLAMA_API_BASE:-http://host.containers.internal:11434/v1}

  # ---------------------------------------------------------------------------
  # LiteLLM proxy — one OpenAI-compatible endpoint (also /v1/messages) for every
  # provider. http://localhost:24000, admin UI at /ui.
  # ---------------------------------------------------------------------------
  litellm:
    image: ghcr.io/berriai/litellm:main-stable
    restart: unless-stopped
    # THIS SERVICE ONLY RUNS IF ITS PROFILE IS ACTIVE. COMPOSE_PROFILES in .env is
    # what activates one — `litellm`, `mlflow`, `litellm,mlflow` or the shorthand
    # `all`. It is compose's OWN variable, not one this repo invented, so
    # `docker compose --profile litellm up -d` does the same thing for one command.
    #
    # THE CONSEQUENCE: with no .env at all, neither gateway starts and `up -d`
    # brings up postgres alone. That is why the quick start begins with
    # `cp .env.example .env`.
    profiles: ["litellm", "all"]
    depends_on:
      postgres: { condition: service_healthy }
      # The config file may be GENERATED, so wait for the generator whether or not
      # it had anything to do. With discovery off that is a one-second no-op; the
      # price of the simpler alternative — no dependency — is litellm crash-looping
      # on a file that arrives a moment later.
      discover: { condition: service_completed_successfully }
    # WHICH ALIASES THIS GATEWAY SERVES, in one filename built from ONE word in .env.
    # GATEWAY_ENGINE holds `lms`, `unsloth`, `ollama`, `openrouter` or `openai`, and
    # litellm/ carries one self-contained config per engine:
    #
    #   litellm/lms.yaml         3 aliases: 2 chat, 1 embedder      free
    #   litellm/unsloth.yaml     3 aliases: 2 chat, 1 embedder      free
    #   litellm/ollama.yaml      3 aliases: 2 chat, 1 embedder      free
    #   litellm/openrouter.yaml  2 aliases: 2 chat                  PAID
    #   litellm/openai.yaml      2 aliases: 1 chat, 1 embedder      PAID
    #
    # Each one includes litellm/settings.yaml for the three settings blocks and then
    # declares its own model_list. There is no fragment/composed split, no generated
    # file and no list: ONE ENGINE RUNS AT A TIME. THE SAME WORD picks mlflow-seed's
    # endpoints below, so the two gateways cannot serve different engines.
    #
    # A TYPO IS A CLEAN CRASH, not a silent one: the file simply does not exist and
    # litellm exits saying so. (It was NOT clean before 2026-08-31, when this mounted
    # a single file: compose bind-mounts a missing source by CREATING AN EMPTY
    # DIRECTORY, and the proxy then crash-looped on `IsADirectoryError` while
    # `compose ps` still said Up. Mounting the directory is what removed that trap.)
    #
    # THE `discovered-` PREFIX IS THE WHOLE AUTO-DISCOVERY SWITCH. `${VAR:+text}`
    # substitutes `text` when VAR is set and NON-EMPTY, so ONE word in .env moves the
    # filename and no second variable can fall out of step with it:
    #
    #   GATEWAY_DISCOVERY unset/empty  ->  /app/config/lms.yaml             hand-written
    #   GATEWAY_DISCOVERY=on           ->  /app/config/discovered-lms.yaml  + every model
    #
    # IT REACTS TO THE WORD BEING NON-EMPTY, NOT TO ITS MEANING, so `GATEWAY_DISCOVERY=off`
    # switches discovery ON here. The `discover` service catches those four words and
    # exits 2 saying to leave the value empty, which is why litellm waits on it.
    command: ["--config", "/app/config/${GATEWAY_DISCOVERY:+discovered-}${GATEWAY_ENGINE:-lms}.yaml", "--port", "4000", "--num_workers", "1"]
    ports:
      - "24000:4000"
    volumes:
      # The whole directory, because every engine config carries
      # `include: [settings.yaml]` and that relative path must resolve next to
      # it. Mount the one engine file alone and the settings vanish. It lands on /app/config
      # and NOT on /app/litellm: the image already ships /app/litellm, which is the
      # proxy's own Python package, and mounting over it breaks the container.
      - ./litellm:/app/config:ro
    extra_hosts:
      # Lets the container reach LMStudio on the host under Docker (Podman adds
      # host.containers.internal automatically; this covers both).
      - "host.docker.internal:host-gateway"
      - "host.containers.internal:host-gateway"
    environment:
      # The ADMINISTRATIVE credential: it mints virtual keys and has no spending
      # ceiling of its own. Callers get a budgeted virtual key, never this.
      LITELLM_MASTER_KEY: ${LITELLM_MASTER_KEY:-sk-litellm-master}
      # REQUIRED, not optional. Without it the proxy boots in no-DB mode: completions
      # keep working while `/key/generate` fails with {"error":"No connected db."} —
      # the worst failure mode for a budget guardrail, because callers then proceed
      # uncapped and nothing looks broken.
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/litellm
      # store_prompts_in_spend_logs (config.yaml) truncates each stored string at
      # 2048 chars by default, which clips agent transcripts mid-run.
      MAX_STRING_LENGTH_PROMPT_IN_DB: "100000"
      # LMStudio on the host (native). Overridable in .env.
      LM_STUDIO_API_BASE: ${LM_STUDIO_API_BASE:-http://host.containers.internal:1234/v1}
      # Unsloth Studio on the host (native), the second local runtime. It binds
      # 127.0.0.1 exactly as LMStudio does, and reaches the containers the same
      # way — verified from inside this container, 2026-08-26.
      #
      # UNLIKE LMStudio it needs a key: every route answers `401 Not
      # authenticated` without one. It is a personal credential, so it arrives
      # from the shell and the line in .env stays blank — see .env.example.
      UNSLOTH_API_BASE: ${UNSLOTH_API_BASE:-http://host.containers.internal:8888/v1}
      UNSLOTH_API_KEY: ${UNSLOTH_API_KEY:-}
      # Ollama on the host (native), the third local runtime, serving the `ollama-*`
      # aliases. It binds 127.0.0.1 like the other two and is reached the same way.
      #
      # NO KEY VARIABLE, and that is not an omission: Ollama does not read the
      # Authorization header at all. The literal `sk-ollama` in both configs is
      # there only so the MLflow seed does not skip the routes — see the note on
      # the Ollama block in litellm/ollama.yaml.
      OLLAMA_API_BASE: ${OLLAMA_API_BASE:-http://host.containers.internal:11434/v1}
      # The two PAID engines: OPENROUTER_API_KEY backs every `openrouter-*` alias and
      # OPENAI_API_KEY every `openai-*` one. Neither engine is in the running config at
      # all unless GATEWAY_ENGINE names it. HF_TOKEN backs nothing now — HuggingFace only
      # ever served `standard-hf`, a fallback target, and no alias has a fallback chain.
      #
      # Blank in .env on this machine: ~/Projects/.envrc already exports the real values
      # from ~/.secrets/secrets.enc.yaml, and compose's shell environment wins over the
      # .env file. See .env.example.
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      HF_TOKEN: ${HF_TOKEN:-}
    healthcheck:
      # /health/liveliness is unauthenticated — it answers without the master key,
      # which /health does not. urllib rather than curl: the base image ships python
      # and is not guaranteed to ship curl.
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:4000/health/liveliness').status==200 else 1)"]
      interval: 10s
      timeout: 5s
      retries: 12
      # First boot runs the LiteLLM schema migrations against an empty database.
      start_period: 60s

  # ---------------------------------------------------------------------------
  # MLflow — the SAME aliases, through the MLflow AI Gateway.
  # http://localhost:25000, UI at / and the gateway under /gateway.
  #
  # The gateway IS the tracking server: there is no second process, and a config
  # change needs no restart. That is also why it cannot be configured by a file —
  # its endpoints live in the `mlflow` database and arrive over an API, which is
  # what mlflow-seed below is for.
  #
  # Call it exactly like LiteLLM, with one URL changed:
  #   POST http://localhost:25000/gateway/mlflow/v1/chat/completions
  #   {"model": "lms-4b", "messages": [...]}
  # ---------------------------------------------------------------------------
  mlflow:
    image: ghcr.io/mlflow/mlflow:latest
    restart: unless-stopped
    # Same rule as litellm above: COMPOSE_PROFILES in .env decides whether this
    # gateway runs at all. `COMPOSE_PROFILES=mlflow` gives you 25000 and no 24000 —
    # which works, because nothing in mlflow/ reads anything in litellm/.
    profiles: ["mlflow", "all"]
    depends_on:
      postgres: { condition: service_healthy }
    command:
      - mlflow
      - server
      # A SQL backend is required: the gateway cannot run on a file store at all.
      - --backend-store-uri
      - postgresql://postgres:postgres@postgres:5432/mlflow
      - --default-artifact-root
      - mlflow-artifacts:/
      - --artifacts-destination
      - /mlartifacts
      - --serve-artifacts
      - --host
      - 0.0.0.0
      - --port
      - "5000"
    ports:
      - "25000:5000"
    volumes:
      - mlflow_artifacts:/mlartifacts
    extra_hosts:
      # THE SERVER dials LMStudio, not the caller — so the local aliases resolve
      # host.containers.internal inside THIS container.
      - "host.docker.internal:host-gateway"
      - "host.containers.internal:host-gateway"
    environment:
      # MLflow 3.x rejects an unknown Host header with 403 "Invalid Host header -
      # possible DNS rebinding attack detected". Requests from the host pass by
      # default; a caller inside the compose network addressing `mlflow:5000` does
      # not, and 0.0.0.0:5000 is the SERVER CALLING ITSELF, which is easy to miss
      # because nobody configures it. Only /health and /version are exempt, so the
      # container looks perfectly healthy while every in-stack call is refused.
      MLFLOW_SERVER_ALLOWED_HOSTS: "mlflow:5000,localhost:5000,localhost:25000,127.0.0.1:5000,127.0.0.1:25000,0.0.0.0:5000"
      # LiteLLM carries `timeout: 3600` on each LMStudio route; MLflow has one
      # global figure and defaults to 300 s. Prompt processing on this machine
      # measures ~100 tok/s, so an agent-scale prompt needs 5-15 MINUTES before
      # its first token — at 300 s the gateway gives up mid-prompt.
      MLFLOW_GATEWAY_ROUTE_TIMEOUT_SECONDS: "3600"
      # Wraps the key that encrypts the stored provider credentials. Blank is
      # supported: MLflow then uses a built-in passphrase and says so in the log.
      # Set it and NEVER change it — the existing secrets stop decrypting, and the
      # fix is to re-run mlflow-seed, which rewrites them.
      MLFLOW_CRYPTO_KEK_PASSPHRASE: ${MLFLOW_CRYPTO_KEK_PASSPHRASE:-}
    healthcheck:
      # /health is one of the two routes exempt from the Host header check above.
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:5000/health').status==200 else 1)"]
      interval: 10s
      timeout: 5s
      retries: 12
      # First boot runs the MLflow schema migrations against an empty database.
      start_period: 60s

  # ---------------------------------------------------------------------------
  # mlflow-seed — writes MLflow's OWN endpoint list into the gateway, then exits.
  #
  # A CONTAINER THAT EXITS IS THE EXPECTED STATE. `compose ps` shows it as exited
  # with code 0 once it has run; `compose logs mlflow-seed` is the record of what
  # it built. It runs again on every `up -d` and is idempotent.
  #
  # MLFLOW'S ALIAS LIST IS PYTHON, NOT YAML, because MLflow's gateway has no config
  # file: its endpoints live in the database and arrive over an API. The list is
  # split exactly the way LiteLLM's YAML is — one self-contained file per engine —
  # and seed.py loads the ONE that GATEWAY_ENGINE names:
  #   mlflow/{lms,unsloth,ollama}.py      3 endpoints each      free
  #   mlflow/openrouter.py                1 endpoint            PAID
  #   mlflow/openai.py                    2 endpoints           PAID
  # They import mlflow/gateway.py, which holds the API calls, so the whole
  # directory is mounted rather than a single file.
  #
  # THE WORD ARRIVES AS AN ENVIRONMENT VARIABLE HERE, not in the filename. That is
  # what lets seed.py validate it and print the allowed values, instead of failing
  # with `python: can't open file`.
  #
  # NOTHING HERE READS A LITELLM FILE. Delete the `litellm` service and the
  # litellm/ directory and this still works. The cost is that an alias must be
  # added in two places — see the header of seed.py.
  # ---------------------------------------------------------------------------
  mlflow-seed:
    image: ghcr.io/mlflow/mlflow:latest
    restart: "no"
    # It seeds the mlflow gateway, so it belongs to the mlflow profile and nothing
    # else. With COMPOSE_PROFILES=litellm it never runs, which is correct: there is
    # no MLflow to seed.
    profiles: ["mlflow", "all"]
    depends_on:
      mlflow: { condition: service_healthy }
    command: ["python", "/app/mlflow/seed.py"]
    volumes:
      - ./mlflow:/app/mlflow:ro
      # The probing, shared with the `discover` service above. MOUNTED READ-ONLY and
      # importable only because of PYTHONPATH below: this container writes no config
      # file, because MLflow has none — its endpoints are database rows.
      - ./discover:/app/discover:ro
    extra_hosts:
      # NEW, AND LOAD-BEARING SINCE DISCOVERY. This container used to talk to nothing
      # but the MLflow server; with GATEWAY_DISCOVERY set it dials the engine on the
      # host to ask what it holds, exactly as the gateways do.
      - "host.docker.internal:host-gateway"
      - "host.containers.internal:host-gateway"
    environment:
      MLFLOW_TRACKING_URI: http://mlflow:5000
      # /app/discover ALONE, never /app. `/app/mlflow` is a directory on that path,
      # and Python would import it as a namespace package called `mlflow` — shadowing
      # the real library that gateway.py imports.
      PYTHONPATH: /app/discover
      # Empty by default: the hand-written mlflow/<engine>.py is then the whole list,
      # which is the state a fresh clone is in. Set it and every model the engine
      # holds is ADDED to that list.
      GATEWAY_DISCOVERY: ${GATEWAY_DISCOVERY:-}
      # WHICH ENDPOINTS THIS RUN WRITES. The SAME word that names LiteLLM's config
      # file above, so the two gateways cannot serve different engines. One engine at
      # a time; seed.py rejects a list and rejects a typo, by name.
      GATEWAY_ENGINE: ${GATEWAY_ENGINE:-lms}
      # The same values LiteLLM gets. This container never calls a provider; it
      # only reads these and hands them to MLflow, which stores them encrypted. A
      # name missing here is the silent failure: the seed script skips the endpoint
      # with "no API key in the environment" and MLflow never gets it.
      LM_STUDIO_API_BASE: ${LM_STUDIO_API_BASE:-http://host.containers.internal:1234/v1}
      UNSLOTH_API_BASE: ${UNSLOTH_API_BASE:-http://host.containers.internal:8888/v1}
      UNSLOTH_API_KEY: ${UNSLOTH_API_KEY:-}
      OLLAMA_API_BASE: ${OLLAMA_API_BASE:-http://host.containers.internal:11434/v1}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      HF_TOKEN: ${HF_TOKEN:-}

volumes:
  postgres_data:
  mlflow_artifacts:
